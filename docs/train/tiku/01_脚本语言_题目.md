# 脚本语言题目

说明：
- 单选题 30 道，其中简单 10 道、中等 10 道、困难 10 道。
- 多选题 15 道，其中简单 5 道、中等 5 道、困难 5 道。

## 单选题

### 简单

1. 下列关于 Shell 的说法，正确的是：
A. Shell 是命令解释器的总称
B. Shell 只是一种文本编辑器
C. Shell 只能在 Windows 中使用
D. Shell 不能调用系统程序

2. 想让脚本中导出的环境变量在当前终端会话中立即生效，最合适的执行方式是：
A. bash env.sh
B. source env.sh
C. ./env.sh
D. nohup env.sh &

3. 在 Shell 脚本中，变量 $? 表示：
A. 当前脚本名
B. 当前用户 UID
C. 上一条命令的退出码
D. 参数个数

4. 在 Bash 脚本中，适合安全透传全部位置参数的写法是：
A. "$*"
B. $*
C. $(*)
D. "$@"

5. 执行 chmod +x script.sh 的主要作用是：
A. 为脚本增加可执行权限
B. 把脚本转换成二进制程序
C. 让脚本自动进入后台
D. 删除脚本中的语法错误

6. 下列命令中，更适合做简单文本过滤的是：
A. passwd
B. grep
C. tar
D. mount

7. crontab 表达式 */5 * * * * 的含义是：
A. 每 5 天执行一次
B. 每分钟执行 5 次
C. 每 5 分钟执行一次
D. 每 5 小时执行一次

8. 在桌面运维场景中，若需要处理 JSON 接口返回并生成结构化报表，更适合优先使用：
A. ping
B. reboot
C. chmod
D. Python

9. Python 虚拟环境的主要价值是：
A. 隔离不同项目的依赖
B. 替代操作系统用户权限
C. 增加 CPU 主频
D. 让脚本自动具备管理员权限

10. Python 中 subprocess.run 的 returncode 字段主要用于：
A. 显示命令彩色输出
B. 判断外部命令是否执行成功
C. 获取 Python 版本号
D. 返回日志文件路径

### 中等

11. 某 Shell 脚本中有一行 ls $file_name，而变量 file_name 的值为 /tmp/my log.txt。此时最可能出现的问题是：
A. Shell 自动把路径转为绝对路径
B. Shell 会把文件复制到当前目录
C. Shell 会把带空格的路径拆成多个参数
D. Shell 会自动进入交互调试模式

12. 在 Shell 脚本开头加入 set -euo pipefail，主要目的是：
A. 强制脚本输出彩色日志
B. 禁止脚本使用函数
C. 让所有命令都在后台并发执行
D. 提高脚本对错误和未定义变量的敏感度

13. 当需要在 Python 中安全执行 systemctl status sshd 并读取结果时，更推荐的写法是：
A. subprocess.run(["systemctl", "status", "sshd"], capture_output=True, text=True)
B. os.system(["systemctl", "status", "sshd"])
C. subprocess.run("systemctl status sshd", shell=True)
D. eval("systemctl status sshd")

14. Python 代码中 with open(path, "r", encoding="utf-8") as f: 的主要优势是：
A. 自动为文件做加密
B. 自动关闭文件句柄
C. 自动创建备份文件
D. 自动把文本转换为 JSON

15. 如果任务是批量调用 HTTP API、解析 JSON 并导出 CSV，更合适的组合是：
A. 仅使用 grep 和 awk
B. 仅使用 crontab
C. 使用 Python requests、json、csv 等能力
D. 仅使用 chmod 和 chown

16. 在 Shell 脚本中使用 trap 'rm -f "$tmp_file"' EXIT 的主要作用是：
A. 把脚本永久写入 /etc/profile
B. 让脚本忽略所有异常
C. 自动为脚本加执行权限
D. 在脚本退出时清理临时文件

17. 下面这段判断更适合用来做什么？ if [[ -z "$1" ]]; then echo "usage"; exit 1; fi
A. 判断第一个参数是否为空
B. 判断上一条命令是否执行成功
C. 判断系统磁盘是否已满
D. 判断脚本是否在 root 用户下运行

18. 在 Python 的 subprocess 中滥用 shell=True 的主要风险是：
A. 所有命令都会自动超时
B. 更容易引入命令注入问题
C. 无法读取标准输出
D. Python 无法调用系统命令

19. 同一脚本手工执行正常，但在 crontab 中失败，最常见的根因之一是：
A. cron 只能执行二进制程序
B. cron 会自动删除脚本
C. cron 默认环境变量较少，PATH 等环境不同
D. crontab 不支持绝对路径

20. 对于长期维护的运维脚本，logging 相比 print 的核心优势是：
A. logging 只能用于 Windows
B. print 无法输出到屏幕
C. print 不能输出中文
D. logging 能记录结构化、分级的运行信息

### 困难

21. 某运维人员执行 bash env.sh 后发现 PATH 仍未改变，但执行 source env.sh 后正常。最准确的解释是：
A. bash env.sh 会在子 Shell 中执行，环境修改不会保留在当前会话
B. bash env.sh 会忽略 export
C. source env.sh 会自动获得 root 权限
D. source env.sh 会把脚本编译成二进制

22. 一条管道命令 cmd1 | cmd2 | cmd3 的最后输出正常，但实际上 cmd1 已失败。若希望脚本把整条管道视为失败，最合适的做法是：
A. 只加 set -u
B. 加 set -o pipefail
C. 在脚本最后加 exit 0
D. 只加 set +e

23. 桌面运维需要写一个工具：从资产接口拉取数据，按部门聚合，生成日报并写入 JSON 文件。下面最合适的语言选择是：
A. 仅用 Shell 别名
B. 仅用 crontab 表达式
C. Python 为主，必要时配合 Shell 调命令
D. 仅用 sed 替换文本

24. 在 Shell 中，下面哪种场景最应该优先使用 "$@" 而不是 "$*"？
A. 需要把全部参数拼成一个长字符串
B. 只想得到参数个数
C. 只想读取第一个参数
D. 透传包含空格的多个原始参数给另一个命令

25. 批量巡检 300 台主机时，大部分时间耗在等待网络返回而不是本地 CPU 计算。若用 Python 实现并发，最合理的性能判断是：
A. 这是典型 I/O 密集型任务，可考虑多线程或异步
B. 这是典型 CPU 密集型任务，应优先多进程
C. 这是磁盘压缩任务，应优先 tar
D. 这是显卡密集型任务，应优先 GPU

26. 需要把用户输入的服务名传给 Python 子进程执行 systemctl status。下面哪种写法安全性最好？
A. subprocess.run(f"systemctl status {service}", shell=True)
B. subprocess.run(["systemctl", "status", service], capture_output=True, text=True)
C. os.system("systemctl status " + service)
D. eval("systemctl status " + service)

27. 某脚本需要在异常退出时删除临时文件、记录错误日志，并保留非零退出码。以下思路最合理的是：
A. 把所有命令输出重定向到 /dev/null
B. 只在脚本第一行写 echo start
C. 使用 set -euo pipefail 配合 trap 做清理和日志处理
D. 遇到任何错误都强制 exit 0

28. 在 Python 中，某脚本需要同时读取 stdout、stderr，并限制单个外部命令 5 秒内返回。最合适的做法是：
A. print("run")
B. open("cmd")
C. 只用 input()
D. subprocess.run([...], capture_output=True, text=True, timeout=5)

29. 为了让团队成员在另一台机器上复现相同 Python 依赖环境，最关键的文件是：
A. requirements.txt
B. README 的标题行
C. crontab -l 的输出截图
D. /etc/passwd

30. 某脚本在交互终端里执行成功，但被计划任务调用时提示 command not found。结合桌面运维常见经验，最佳修复方向是：
A. 直接重装操作系统
B. 在脚本中使用命令绝对路径，并补齐必要环境变量
C. 把脚本改成 .txt 文件
D. 删除全部日志后重试

## 多选题

### 简单

31. 下列哪些是 Shell 中的特殊参数？
A. 打印机墨盒校准
B. $#
C. $?
D. $0

32. 下列哪些 Python 标准库或常用模块常用于运维脚本的文件与数据处理？
A. pathlib
B. json
C. FC SAN Zoning
D. subprocess

33. 关于 Shell 引号的说法，正确的有：
A. 不加引号处理带空格路径有风险
B. FC SAN Zoning
C. 单引号通常不会展开变量
D. 双引号会保留大多数变量边界

34. 下列哪些场景更适合优先使用 Python 而不是纯 Shell？
A. 域控 OU 复制
B. 解析 JSON 接口返回
C. 组织结构化数据输出
D. 处理复杂异常和重试逻辑

35. 关于计划任务和自动化，正确的有：
A. cron 适合固定周期任务
B. systemd timer 也可用于定时执行
C. 计划任务中最好尽量使用绝对路径
D. 仅用于办公区装修的管理动作

### 中等

36. 关于 Shell 健壮性写法，合理的有：
A. 与题干主题无关的桌面美化项
B. set -u 可帮助发现未定义变量
C. trap 可在退出时清理临时资源
D. pipefail 可避免管道前段失败被掩盖

37. 关于 Python 中调用系统命令的推荐实践，正确的有：
A. 优先使用列表形式传参
B. 需要关注 returncode
C. 应根据场景读取 stdout 与 stderr
D. RAID 阵列重建

38. 下列哪些情况说明 Python 更适合承担主流程而不是 Shell？
A. 需要做较复杂的异常分支控制
B. 需要实现复杂参数解析
C. 交换机 Trunk 封装
D. 需要处理 API 会话和认证

39. 关于日志与输出，正确的有：
A. 生产脚本最好记录关键动作和失败原因
B. 只要脚本执行成功，就完全不需要日志
C. logging 适合长期维护脚本
D. print 更适合临时调试输出

40. 下列哪些是导致脚本可维护性下降的常见问题？
A. 完全不做参数化
B. 把密码硬编码在源码中
C. 把依赖版本管理留给“大家自己记住”
D. 域控 OU 复制

### 困难

41. 某脚本需要长期运行在多环境中，既要避免依赖冲突，又要支持参数化执行和日志追踪。较优设计包括：
A. 使用 argparse 或配置文件管理参数
B. 使用虚拟环境隔离依赖
C. 把所有环境差异都写死在源码里
D. 使用 logging 记录关键过程

42. 批量巡检脚本需要从 200 台主机读取服务状态并输出报告。为降低风险，合理做法包括：
A. 对失败主机记录上下文信息
B. 为远程请求设置超时
C. 某一台主机失败时无条件吞掉异常且不留痕
D. 用结构化格式输出结果

43. 关于 source 与直接执行脚本的差异，正确的有：
A. 两者对当前会话环境的影响一定完全相同
B. source 常用于加载环境变量
C. 直接执行脚本常常会启动新进程或新解释器上下文
D. source 在当前 Shell 中执行

44. 下列哪些做法能有效降低 Python 调命令时的安全风险？
A. 用列表形式传递参数
B. 尽量避免 shell=True
C. 把任意用户输入直接拼进命令字符串再执行
D. 对用户输入做必要校验

45. 某团队要交付一个可复用的桌面运维巡检工具，以下哪些要求是合理的？
A. 关键依赖应可复现
B. 题目或结果输出最好结构化
C. 打印机墨盒校准
D. 脚本应能明确返回成功或失败状态
