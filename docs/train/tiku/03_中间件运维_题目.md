# 中间件运维题目

说明：
- 单选题 30 道，其中简单 10 道、中等 10 道、困难 10 道。
- 多选题 15 道，其中简单 5 道、中等 5 道、困难 5 道。

## 单选题

### 简单

1. Nginx 在桌面运维与平台运维中最常见的定位是：
A. Web 入口层与反向代理
B. BIOS 管理工具
C. Java 开发 IDE
D. 关系型数据库

2. 在 Nginx 配置中，proxy_pass 指令的主要作用是：
A. 重建 RAID
B. 指定后端转发目标
C. 清空访问日志
D. 修改 Linux 用户密码

3. 下列关于 Redis 的说法，正确的是：
A. Redis 只能部署在 Windows 中
B. Redis 只能存图片，不能存字符串
C. Redis 是典型的内存型键值数据库
D. Redis 只能用于物理备份

4. Redis 之所以常被用作缓存，最核心的原因之一是：
A. 它不能处理并发请求
B. 它主要依赖磁带存储
C. 它只能保存 XML
D. 它以内存读写为主，访问速度快

5. Tomcat 的基本定位是：
A. Java Web 容器
B. 终端准入系统
C. 光纤交换机
D. 二层交换机

6. Tomcat 中通常用于部署应用的目录是：
A. bin/cache
B. webapps
C. logs
D. temp

7. TongWeb 更常见于哪类场景？
A. 个人博客图片压缩
B. 仅用于 BIOS 升级
C. 强调国产化与企业级合规的项目
D. 蓝牙驱动开发

8. Redis 的默认服务端口通常是：
A. 80
B. 22
C. 1521
D. 6379

9. 当需要查看 Nginx 的访问状态码与请求路径时，最先应关注的日志是：
A. access.log
B. crontab
C. boot.log
D. shadow

10. 若要在修改 Nginx 配置后先检查语法是否正确，应优先执行：
A. kill -9 nginx
B. nginx -t
C. format c:
D. rm -rf /etc/nginx

### 中等

11. Nginx 返回 502 Bad Gateway，最常见的原因之一是：
A. DNS 一定配置为 8.8.8.8
B. 交换机一定坏了
C. 上游服务异常或不可达
D. 浏览器收藏夹损坏

12. Nginx 返回 504 Gateway Timeout，更贴近的含义是：
A. 客户端本地磁盘损坏
B. 当前用户密码过期
C. 数据库表空间已满
D. 上游响应过慢导致网关超时

13. 用户通过 Nginx 上传大文件时被拒绝，最应优先检查的配置项是：
A. client_max_body_size
B. user nginx
C. gzip_types
D. server_tokens

14. 在“会话缓存、计数器、排行榜”这类场景下，通常更适合优先考虑：
A. RAID 1
B. Redis
C. IPMI
D. BIOS

15. 关于 Redis 单线程模型的理解，最准确的是：
A. 单线程意味着 Redis 一定很慢
B. Redis 只要单线程就完全没有并发能力
C. Redis 在内存场景下通过简单结构和 I/O 多路复用保持高性能
D. Redis 单线程意味着不能做持久化

16. Tomcat 启动失败时，如果怀疑端口冲突，最应优先检查的是：
A. Office 是否激活
B. 浏览器是否装了插件
C. 显卡驱动是否更新
D. 8080 或相关配置端口是否已被占用

17. 在 Nginx + Tomcat 的典型组合中，更常见的职责分工是：
A. Nginx 负责入口代理，Tomcat 负责运行 Java Web 应用
B. 两者都只负责桌面壁纸策略
C. Tomcat 负责四层交换，Nginx 负责 RAID
D. Nginx 负责存储 SQL 数据，Tomcat 负责打印

18. TongWeb 与 Tomcat 相比，更容易出现在国产化项目中的原因之一是：
A. TongWeb 只能处理静态文件
B. TongWeb 更强调企业级能力与国产生态适配
C. TongWeb 不需要 JDK
D. Tomcat 一定不能跑 Java 应用

19. 如果 Redis 对公网暴露且未做认证与访问控制，最主要的风险是：
A. 自动开启主从复制
B. 自动修复内存泄漏
C. 容易被未授权访问和恶意利用
D. 自动获得更高缓存命中率

20. 对 Nginx 做配置变更时，最标准的操作顺序是：
A. 先删日志，再改配置
B. 直接重装系统
C. 改配置后立刻 kill 进程
D. 改配置，执行 nginx -t，通过后再 reload

### 困难

21. 对于以下 Nginx location 规则，优先级最高的是：
A. location = /login
B. location ^~ /static/
C. location /api/
D. location ~ \.php$

22. 在反向代理场景中，如果后端应用需要拿到客户端真实来源 IP，通常应正确传递的头包括：
A. Hostname-Color
B. X-Real-IP 或 X-Forwarded-For
C. BIOS-Version
D. RAID-State

23. Redis 实例内存已接近上限且未设置合适的淘汰策略，继续写入时最可能出现：
A. Redis 自动切换成 Oracle
B. 日志自动归档到 RMAN
C. 写请求失败或缓存行为不可控
D. 所有 Key 自动永久保留且不占内存

24. 某同学认为“Redis 是单线程，所以一定不适合高并发”。最合理的反驳是：
A. Redis 从不处理网络 I/O
B. Redis 依赖磁盘顺序读写，所以单线程更快
C. 只要是单线程程序都一定比多线程快
D. Redis 的性能关键在内存访问、简单数据结构和事件模型，而不是线程数越多越好

25. Tomcat 应用频繁出现 Full GC，页面偶发卡顿。运维排查时除了应用日志，更应重点关注：
A. JVM 堆参数与 GC 日志
B. 机房门禁权限
C. 机械键盘驱动
D. 显示器分辨率

26. 在安全要求较高的环境中，Tomcat 的 AJP 端口常被禁用或严格限制，主要原因是：
A. AJP 只能用于打印服务
B. 该通道可能带来额外暴露面与安全风险
C. AJP 会禁用 JVM
D. AJP 会自动删除日志

27. 某金融项目需要国产操作系统、国产数据库、企业级集群与统一管理控制台。就应用服务器选型而言，更合理的是：
A. 一律改用 FTP
B. 只使用 telnet
C. 优先评估 TongWeb 等国产企业级应用服务器
D. 仅靠浏览器缓存替代应用服务器

28. 当后端各节点请求处理时长差异较大时，Nginx upstream 更适合优先考虑的调度策略是：
A. backup
B. ip_hash
C. server_tokens off
D. least_conn

29. 在 Nginx access.log 中看到大量 499 状态码，更贴近的含义是：
A. 客户端主动断开连接
B. 数据库主备切换成功
C. 证书一定过期
D. 系统磁盘一定已满

30. 某系统前端经 Nginx 访问时返回 502，Tomcat 进程存在且端口也监听。此时最合理的下一步排查是：
A. 直接把 Nginx 改成数据库
B. 检查 upstream 配置、代理路径、应用日志和后端实际响应
C. 立即格式化服务器
D. 删除全部日志避免占空间

## 多选题

### 简单

31. 下列哪些是 Nginx http 场景中的常见配置块？
A. 不属于该知识点范围的干扰项
B. server
C. location
D. http

32. Redis 的常见应用场景包括：
A. 缓存
B. 仅用于办公区装修的管理动作
C. 计数器
D. 排行榜

33. 下列哪些 Tomcat 目录与用途匹配正确？
A. 不属于该知识点范围的干扰项
B. conf 用于配置文件
C. logs 用于日志文件
D. webapps 用于应用部署

34. 关于 TongWeb 的说法，正确的有：
A. 常与国产数据库、国产操作系统一起出现
B. 运维时也要关注 JDK 兼容性
C. 常见于国产化和合规场景
D. 仅用于办公区装修的管理动作

35. Nginx 变更时的合理做法包括：
A. BMC 带外升级
B. 变更后先执行 nginx -t
C. 语法通过后平滑 reload
D. 关注 access.log 和 error.log 验证效果

### 中等

36. 下列哪些情况可能导致 Nginx 返回 502 或 504？
A. 与题干主题无关的桌面美化项
B. 后端处理过慢
C. 上游服务未启动
D. 上游端口配置错误

37. 关于 Redis 安全加固，合理的有：
A. 避免直接暴露公网
B. 关注持久化与高危命令控制
C. 限制来源访问
D. AD 域组策略下发

38. 在反向代理场景下，为保留原始访问信息，常见做法包括：
A. 视场景设置超时参数
B. 传递 X-Forwarded-For
C. 打印机驱动签名校验
D. 传递 Host 头

39. Tomcat 运维排查时，常见关注点包括：
A. JVM 内存参数是否合理
B. catalina.out 或应用日志是否报错
C. 打印机驱动签名校验
D. 端口是否冲突

40. 关于中间件角色定位，正确的有：
A. 打印机驱动签名校验
B. TongWeb 更偏国产企业级应用服务器语境
C. Nginx 强项在入口代理与静态资源处理
D. Tomcat 是 Java Web 容器

### 困难

41. 若要提升 Nginx 的性能与可运维性，可考虑的方向包括：
A. 合理评估 worker_connections
B. 根据场景设置 worker_processes 或 auto
C. 所有参数无脑调到最大即可
D. 配置日志并结合连接数、响应时间排查

42. 关于 Redis 的持久化与高可用理解，正确的有：
A. 不属于该知识点范围的干扰项
B. 缓存场景也不能完全忽视数据安全和恢复策略
C. RDB 和 AOF 都属于常见持久化思路
D. 主从复制可提升可用性与读扩展能力

43. 关于 Tomcat 与 TongWeb 的比较，正确的有：
A. RAID 级别切换
B. TongWeb 更常见于国产化企业项目
C. 两者运维都要关注日志、端口和 JDK 兼容性
D. TongWeb 的集群和管理能力通常是对比重点之一

44. 某系统采用 Nginx 反代 Tomcat，用户偶发投诉“页面慢”。合理排查方向包括：
A. 只看浏览器收藏夹设置即可下结论
B. 检查 upstream、超时和后端响应时长
C. 查看 Tomcat 应用日志与 JVM 状态
D. 查看 Nginx access.log、error.log

45. 对于一个成熟的中间件运维体系，下列哪些目标是合理的？
A. 国产化场景考虑兼容性与集群管理
B. RAID 级别切换
C. 缓存服务有安全与容量治理
D. 入口代理配置可回滚
